# DYAD — 디자인

PLAN.md §3 의 트랙 규칙을 시각 언어로 옮긴 문서. ennea 와 공유하는 것은 없다 (독립).
스타일 타일(눈으로 보는 판)은 `design/style-tile.html`.

---

## 0. 한 줄

**색은 둘, 판정은 밝기, 모양은 곧음/꺾임.**

- 유채색은 **돈 색과 캇 색 둘뿐**이다. 버튼·포커스·난이도·진행바·결과까지 이 둘과 무채색 단계로만 짠다. 세 번째 색이 필요해 보이면 설계가 틀린 것
- **판정(Great/OK/Miss)은 색이 아니라 밝기**로 말한다. 흰 섬광 / 색 그대로 / 회색으로 탈색
- **타입은 실루엣이 1차, 색은 2차.** 돈 = 곧은 막대, 캇 = 꺾인 막대. 색약이어도, 흑백이어도 읽혀야 한다

---

## 1. 색

### 법칙
- 돈 = **앰버** (따뜻함, 저음, 아래 행). 캇 = **페리윙클** (차가움, 고음, 위 행)
- 타이코의 빨강/파랑을 피하되 "따뜻함=돈, 차가움=캇" 관습은 지킨다. 노랑↔파랑 축이라 적록색약에서도 구분됨
- UI 셸에서의 역할: **앰버 = 행동(주 버튼, 확인)**, **페리윙클 = 선택·포커스·정보(현재 항목, 링크, 슬라이더)**. 이 배정을 화면마다 바꾸지 않는다
- 자켓 `palette` 는 **배경 틴트에만** 쓴다. 노트·게이트·텍스트 색을 건드리지 않는다

### 토큰 (다크 = 기본)

| 토큰 | 다크 | 라이트 | 용도 |
|---|---|---|---|
| `void` | `#07070C` | `#C6C5D3` | 레터박스 바깥 |
| `ground` | `#12131F` | `#F6F5FB` | 화면 바탕 |
| `surface` | `#1A1B2B` | `#FFFFFF` | 트랙, 카드, 입력창 |
| `raised` | `#24263A` | `#ECEBF4` | 게이트 셀(대기), 터치존, 버튼(보조) |
| `line` | `#343651` | `#D5D4E2` | 박자선, 구분선, 손 경계 |
| `text` | `#EEEDF8` | `#15152A` | 본문, 점수 |
| `text-dim` | `#A2A3BE` | `#55566E` | 보조 텍스트, 콤보 |
| `text-faint` | `#6C6E8A` | `#80819A` | 비활성, Miss 탈색 |
| `don` | `#FFB432` | `#F0A000` | 돈 노트/셀 채움 |
| `don-ink` | `#FFB432` | `#A66300` | 돈 색 텍스트·아이콘 (라이트에서 대비 확보용) |
| `kat` | `#8FA0FF` | `#7085FF` | 캇 노트/셀 채움 |
| `kat-ink` | `#8FA0FF` | `#3D4ED0` | 캇 색 텍스트·아이콘 |
| `on-don` | `#241600` | `#241600` | 돈 위의 글자 |
| `on-kat` | `#0A0E33` | `#0A0E33` | 캇 위의 글자 |
| `flash` | `#FFFFFF` | `#FFFFFF` | Great 섬광 |

- 무채색은 전부 **파랑-보라로 살짝 기운 회색**. 순수 회색(#888 류)은 쓰지 않는다
- `void` 는 `ground` 보다 확실히 어둡게(다크) / 밝게(라이트) 해서 레터박스가 프레임처럼 보이게 한다

### 판정 = 밝기
| 판정 | 노트 | 게이트 셀 | 텍스트 |
|---|---|---|---|
| Great | `flash` 로 90ms 튀었다가 사라짐 | `flash` → 타입 색으로 120ms 감쇠 | `text` |
| OK | 타입 색 그대로 사라짐 | 타입 색 60% 로 켜짐 | `text-dim` |
| Miss | `text-faint` 로 탈색되어 게이트를 통과 | 반응 없음 | `text-faint` |

### 배경 틴트
- 빌드 시 자켓에서 뽑은 `palette[0]` 을 `ground` 위에 **다크 12% / 라이트 6%** 로 섞어 플레이 배경으로 쓴다. 곡마다 분위기가 바뀌되 노트 대비는 그대로
- 자켓 원본은 곡 선택과 결과에서만 보인다. 플레이 중 배경으로 깔지 않는다 (가독성)

---

## 2. 모양

### 브릭 법칙
- 모든 도형은 **모서리 반경 = 짧은 변 ÷ 8** 인 직사각형(브릭)에서 시작한다. 크기가 위계를 만들고 반경은 만들지 않는다
- 원은 쓰지 않는다. 타이코의 원형 노트와 시각적으로 갈라지는 지점

### 노트 (트랙 로컬 좌표: 진행축 × 폭축)
트랙 폭을 `W`, 노트 두께를 `N = W / 16` 로 둔다. (처음 `W/12` 였으나 실제로 보니 너무 커서 줄임. 수치는 전부 `tokens.ts` 의 `SHAPE` 에 있다)

| | 폭 | 실루엣 |
|---|---|---|
| 돈 | `0.5W`, 중앙 | 곧은 브릭, 두께 `N` |
| 캇 | `0.5W`, 중앙 | 꺾인 브릭(쉐브론). 가운데가 **노트가 오는 쪽**으로 `N` 만큼 솟음 |
| 왕 돈 | `W` | 곧은 브릭 하나, 가운데에 1px `ground` 색 이음선 |
| 왕 캇 | `W` | 쉐브론 둘을 이어 붙인 **M자**, 가운데 이음선 |

- 캇의 꺾임은 위 행(캇)의 "위로 뾰족" 을 진행축으로 옮긴 것. 세로 모드에서는 위, 가로 모드에서는 오른쪽을 가리킨다
- 왕노트가 패드 한 행의 두 칸을 그대로 그린 형태라는 PLAN 의 요구를 이음선이 담당한다
- 링(외곽선) 캇은 기각: 세로 모드에서 외곽선이 얇아져 색 면적이 사라진다

### 게이트 (판정선)
- 2×2 셀. 폭축 = 손(왼/오), 진행축 = 타입. **캇 행은 노트가 오는 쪽**, 돈 행은 가까운 쪽 → 세로 모드에서 패드와 같은 배치(KK 위 / DD 아래)
- 셀 크기 `W/2 × N`, 셀 간격 `N/8`, 대기 색 `raised`, 테두리 `line` 1px
- 판정선은 두 행 사이 이음. 노트 중심이 여기에 닿는 순간이 `t`
- 친 손의 열 × 친 타입의 행 셀 하나가 켜진다. 왕노트 성공은 한 행 두 칸이 같이 켜진다
- 셀 안에 키 라벨(R U / F J)을 `text-faint` 로. 터치 모드에서는 라벨 없음

### 트랙
- 바탕 `surface`, 좌우 바깥은 `ground`(틴트)
- 손 경계: 중앙에 `line` 색 **점선**. 일반 노트가 그 위를 덮으므로 "어느 손" 을 강요하지 않는다는 뜻이 그대로 보인다
- 박자선 `line` 1px, 마디선 `text-faint` 1px. SV 무시, 등속

### 터치존 (세로)
- 하단 35~40%, 2×2. 셀은 게이트와 같은 브릭, 색 `raised`, 눌리면 타입 색 40%
- 셀 가운데에 타입 실루엣 미니어처(곧음/꺾임)를 `text-faint` 로 찍어 무엇을 치는 칸인지 말한다. 글자 없음

---

## 3. 타이포

| 역할 | 서체 | 무게 | 비고 |
|---|---|---|---|
| 점수·콤보·판정·타이틀 | **Unbounded** | 700 (점수), 500 (판정) | 넓고 무거움. 북 같은 무게감. `tabular-nums` |
| 본문·라벨·버튼·곡 정보 | **IBM Plex Sans KR** | 400 / 500 | 한글 포함. 라벨에 대문자 안 씀 |

- 서체는 이 둘뿐. 모노스페이스 없음
- 점수는 항상 7자리 고정폭 (`0,987,650` 이 아니라 앞을 비운 `  987,650`). 자리수가 바뀌어도 폭이 흔들리지 않는다
- 판정 텍스트는 "Great / OK / Miss" 영문 고정. 한 프레임에 읽혀야 하므로 번역하지 않는다
- 난이도는 **EASY / NORMAL / HARD** 세 단계. 타일에 이 이름과 1~10 레벨 숫자(Unbounded)를 함께 적는다. osu `Version` 문자열은 보조 이름으로만 보여 준다. (대문자 라벨 금지 규칙의 유일한 예외: 단계 이름은 라벨이 아니라 고유명으로 취급)

### 스케일 (논리 해상도 기준 px)
`12 · 14 · 16 · 20 · 28 · 40 · 64 · 96`. 점수 64, 판정 40, 콤보 28, 곡 제목 20, 본문 16, 보조 14, 키 라벨 12.

---

## 4. 간격·레이아웃

- 8px 격자. 트랙·게이트·터치존은 `W`, `N` 파생값이라 격자에 맞추지 않는다
- **가로는 1280×720 고정.** 창이 크면 바깥은 `void` 레터박스, 1배를 넘겨 키우지 않는다. 트랙 밴드 높이 320px(`W`, N = 20) 를 세로 중앙에, 위 띠에 자켓(소)·제목·점수, 아래 띠에 콤보. 게이트는 왼쪽 20% 지점. 판정 텍스트는 밴드 **위쪽** 띠에, 판정선 x 에 맞춰 가운데 정렬
- **세로는 동적 해상도.** 논리 폭 720 고정, 논리 높이 = 720 × 화면 비율(960~1920 클램프). 캔버스가 화면을 꽉 채우고 레터박스 없음. 위에서부터 정보 띠 112px(고정) / 트랙 / 게이트 / 터치존 = 높이의 30%(최소 360px). 긴 화면일수록 트랙이 길어진다
- 선행 시간(hiSpeed 1): 세로 1200ms, 가로 1600ms. 한 화면에 150 BPM 기준 4~6박이 보인다. 수치는 `tokens.ts` `LAYOUT.leadMs`
- 텍스트는 왼쪽 정렬. 점수와 콤보만 오른쪽 정렬(자릿수 고정). 가운데 정렬은 판정 텍스트와 곡 선택 캐러셀뿐
- 라운드 카드는 쓰지 않는다
- **곡 선택은 캐러셀.** 가운데 자켓 하나(가로 340px, 폰에서는 화면 폭의 60%), 양옆 자켓은 작아지고 흐려지며 뒤로 물러난다(ennea 와 같은 구조). 아래에 제목(Unbounded), 아티스트, BPM·길이, EASY/NORMAL/HARD 타일(레벨 숫자, 없는 단계는 점선), 노트 수, 최고 기록. 선택된 타일만 `kat` 채움. 좌우 키·휠·스와이프로 곡, 상하 키로 단계, Enter·가운데 탭으로 시작

---

## 5. 모션

- 원칙: **짧고, 답하는 움직임.** 입력·박자·화면 전환에만 답하고, 떠다니는 장식은 없다. 모든 모션은 `MOTION` 토큰의 ms 를 쓴다
- 히트: 노트가 게이트 방향으로 `N/2` 밀리며 90ms 안에 1.2배로 커지며 사라진다. Great 는 그 사이 `flash`
- 히트 버스트: 노트의 **윤곽선**(같은 모양, 왕노트는 큰 모양)이 판정선에서 1.9배까지 커지며 240ms 에 사라진다. Great 는 `flash` 로 시작해 타입색으로, OK 는 타입색 70%, 왕노트 첫 손은 55%, 연타·스피너 틱은 40%
- 판정어: Great 는 1.35배, OK 는 1.15배에서 90ms 에 자리를 잡고 스폰 쪽으로 12px 떠오르며, Miss 는 10px 가라앉는다. 60% 지점부터 페이드
- 박자 펄스: 매 박에 판정선이 `text-dim` → `text` 로 (마디는 100%, 박은 40%) 밝아졌다 220ms 에 돌아오고, 마디에는 트랙 배경이 `surface` → `raised` 60% 까지 들렸다 돌아온다
- 게이트 셀: 켜짐 즉시, 꺼짐 120ms 감쇠. 스케일 바운스 없음
- Miss: 노트가 탈색된 채 등속으로 게이트를 지나 `N × 2` 뒤에서 사라진다. 판정선이 120ms 동안 35% 로 가라앉는다. 빨간 섬광·흔들림 없음
- 콤보 숫자: 매 증가마다 1.12배에서 160ms 에 돌아온다. 50 단위에서는 1.45배 + `text` 로 밝아졌다 360ms 에 `text-dim` 으로
- 셸: 화면은 260ms 페이드 + 12px 상승으로 들어온다 (라우트가 바뀔 때마다 새로 마운트). 브릭 버튼은 hover 에 10% 밝아지고 누르면 0.96배. 티어 타일은 선택 시 1.1배에서 220ms 에 자리를 잡는다. 캐러셀 가운데 자켓은 `kat` 30% 그림자를 두르고, 곡이 바뀌면 제목·정보 밴드가 220ms 로 교체된다
- 부팅: 앱에서 유일하게 연출된 진입. 셰브론과 브릭이 위에서 내려앉고(520ms, 140ms 간격), 워드마크와 버튼이 뒤따라 떠오른다. 이후 두 모양은 4초 주기로 4px 숨을 쉰다
- 결과: 점수가 0 에서 900ms 에 세어 올라가고, 판정 막대가 Great 부터 90ms 간격으로 700ms 에 자라며, 나머지 줄은 70ms 간격으로 떠오른다. 신기록 표시는 카운트업이 끝난 뒤 튀어나온다
- `prefers-reduced-motion` 이면 히트 밀림·팝·버스트·섬광·박자 펄스·판정어 이동을 끄고 즉시 사라짐만 남긴다. 셸의 애니메이션·트랜지션은 전역으로 꺼지고, 결과 화면은 최종값을 바로 보여준다
- 결과 화면에서 유일하게 두 색을 같이 쓴다: 판정 분포 막대는 Great `text`, OK `text-dim`, Miss `text-faint` 이고, 정확도 숫자만 `don-ink`, 신기록 표시만 `kat-ink`

---

## 6. 구현 규칙

- 토큰의 **단일 원천은 `src/design/tokens.ts`**. 다크/라이트 두 객체와 `N = W/12`, 반경 1/8 같은 상수를 여기 둔다
- Svelte 루트가 마운트 시 이 객체를 `document.documentElement.style.setProperty('--don', …)` 로 주입한다. CSS 에 hex 를 직접 쓰지 않는다
- Pixi 렌더러는 같은 `tokens.ts` 를 import 한다. 두 벌의 색 정의를 두지 않는다
- 테마 전환 = 객체 교체 + 재주입. 플레이 중에는 전환하지 않는다
- 폰트는 `@fontsource-variable/unbounded`, `@fontsource/ibm-plex-sans-kr` 로 동봉. CDN 의존 없음 (정적 호스팅, PWA 가능성)
- 자켓 `palette` 는 `meta.json` 의 hex 배열. 배경 틴트 계산은 `tokens.ts` 의 `tint(ground, palette[0], 0.12)` 하나로

---

## 7. 기각한 대안 (기록용)

- **타이코 계승 (빨강/파랑, 원형 노트, 옻칠·나무)**: 즉시 읽히지만 클론처럼 보이고, PLAN 의 막대형 노트와 원형 관습이 충돌
- **리소그래프 2도 인쇄**: 개성은 가장 강하나 그레인·오버프린트가 고속 가독성과 싸운다. 셸에만 쓰고 플레이필드는 평면으로 가면 두 세계가 갈라짐
- **ennea 토큰 체계 공유**: 22% 곡률 법칙이 막대형 노트와 맞지 않고, DYAD 정체성을 약하게 함
- **판정에 세 번째 색(초록/빨강)**: 듀오톤을 깨고, Miss 빨강이 돈 앰버와 섞여 읽힘
- **핑크/시안, 오렌지/틸**: 각각 신스웨이브·시네마틱 기본값. 앰버/페리윙클로 대체

---

## 8. 미확정

- 라이트 테마를 1차 배포에 넣을지 (토큰은 정의해 둔다)
- 캇 쉐브론 솟음 `N` 이 오니 채보 고속에서 충분히 구분되는지 → 마일스톤 3 에서 실측 후 `1.5N` 까지 조정 여지
- 터치존 미니어처를 실루엣만으로 할지, 첫 실행 온보딩 때만 글자(돈/캇)를 겹칠지
